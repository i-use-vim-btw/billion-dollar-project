apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-milvus-db-service
  namespace: {{ .Values.namespace }}
spec:
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  selector:
    app: milvus
    tier: db
  type: NodePort

